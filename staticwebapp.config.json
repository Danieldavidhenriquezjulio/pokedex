{
  "routes": [
    {
      "route": "/pokedex-angular/assets/*",
      "rewrite": "/assets/{*}"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/assets/*", "/*.{png,jpg,gif,ico,css,js}"]
  }
}
